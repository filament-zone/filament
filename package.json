{
  "scripts": {
    "markdownlint": "markdownlint **/*.md --ignore node_modules --ignore target",
    "rustlint": "cargo +nightly fmt --all --check",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@commitlint/cli": "^18.6.0",
    "@commitlint/config-conventional": "^18.6.0",
    "husky": "^9.0.6",
    "markdownlint-cli": "^0.39.0"
  }
}
