{
  "scripts": {
    "markdownlint": "markdownlint **/*.md --ignore node_modules --ignore target --ignore test-data/README.md",
    "rustlint": "cargo +nightly fmt --all --check",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7",
    "markdownlint-cli": "^0.43.0"
  }
}
